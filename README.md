# Focus Todo

A small browser-based todo app with local storage, filters, completion tracking, and a responsive layout.

## Run locally

Open `index.html` directly in a browser, or serve the folder:

```powershell
python -m http.server 8080
```

Then visit `http://localhost:8080`.
